# Leash Server Configuration

# Optional: Set a password to enable authentication for all connections
# If not set, the server will run without authentication (local network only)
# For VPS deployment, this should ALWAYS be set
# LEASH_PASSWORD=your-secure-password-here

# Server port (default: 3001)
# PORT=3001

# VPS Deployment: Custom domain for external connections
# Set this to your VPS domain or public IP address
# Example: leash.example.com or 123.45.67.89
# LEASH_DOMAIN=your-domain.com

# VPS Deployment: External port (if using reverse proxy)
# Set this if your external port differs from PORT (e.g., 443 for HTTPS/WSS)
# LEASH_EXTERNAL_PORT=443
