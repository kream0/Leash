# Leash Server Configuration

# Optional: Set a password to enable authentication for all connections
# If not set, the server will run without authentication (local network only)
# For VPS deployment, this should ALWAYS be set
# LEASH_PASSWORD=your-secure-password-here

# Server port (default: 3001)
# PORT=3001
